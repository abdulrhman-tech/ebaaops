# دليل نظام بيت الإباء لإدارة الحجوزات والمخزون

> دليل شامل بالعربي يشرح آلية عمل النظام بالكامل: الأدوار، إنشاء الطلب، الموافقات، الرفض، صرف المخزون، التمديد، الإشعارات، التقارير، والميزات المساندة.

---

## 1. نظرة عامة

نظام مؤسسي لإدارة حجز البضائع واعتمادها في شركة بيت الإباء. يوفر:
- تسجيل دخول آمن (JWT) مع صلاحيات حسب الدور.
- سير عمل اعتماد متعدد المراحل.
- إدارة صرف المخزون (جزئي أو كامل).
- نظام تمديد للحجوزات.
- إشعارات داخل النظام وعبر البريد الإلكتروني.
- سجل تدقيق كامل لكل العمليات.
- تقارير وتحليلات مفصّلة.
- واجهة ثنائية اللغة (عربي/إنجليزي) مع دعم RTL/LTR.

**التقنيات:** React + TypeScript (الواجهة) — Node.js + Express (الخلفية) — PostgreSQL مع Drizzle ORM — تخزين الملفات في التخزين السحابي (Object Storage) — البريد عبر Resend.

---

## 2. الأدوار والصلاحيات

| الدور | الصلاحيات الرئيسية |
|---|---|
| **مندوب المبيعات (Sales Rep)** | ينشئ الطلبات، يرى طلباته فقط، يعدّل الطلبات المرفوضة ويعيد إرسالها |
| **مدير الفرع (Branch Manager)** | يعتمد/يرفض الطلبات المُقدَّمة لفروعه (يدعم تعدد الفروع للمدير الواحد) |
| **مدير الصنف (Category Manager)** | يعتمد/يرفض الطلبات المعتمدة من الفرع لفئته |
| **مدير القطاع (Sector Head)** | يعتمد الحجوزات التي مدتها أكثر من 90 يوم، ويعتمد الحجوزات الاستراتيجية |
| **مدير التخطيط (Planning)** | الاعتماد النهائي، إنشاء حجز استراتيجي، إنشاء طلب صرف المخزون، إدارة المخازن |
| **المسؤول (Admin)** | صلاحية كاملة على كل شيء |

> ملاحظة: مندوب المبيعات لا يعتمد ولا يرفض إطلاقاً — دوره هو الإنشاء والتعديل وإعادة الإرسال فقط.

---

## 3. حالات الطلب

| الحالة | المعنى |
|---|---|
| `submitted` (مُقدَّم) | الطلب جديد بانتظار مدير الفرع |
| `branch_approved` (معتمد من الفرع) | بانتظار مدير الصنف |
| `category_approved` (معتمد من الصنف) | بانتظار مدير القطاع (إذا > 90 يوم) ثم مدير التخطيط |
| `final_approved` (اعتماد نهائي) | تم الاعتماد، يمكن صرف المخزون |
| `rejected` (مرفوض نهائياً) | فقط عند رفض مدير الفرع — يعدّل المندوب ويعيد الإرسال |
| `expired` (منتهي الصلاحية) | انتهت مدة الحجز ولا تزال هناك بنود غير مصروفة |
| `lost_opportunity` (فرصة ضائعة) | مرّ 3 أيام على الانتهاء بدون تجديد (تلقائي) |

---

## 4. سير عمل إنشاء الطلب

### 4.1 إنشاء طلب عادي (مندوب المبيعات)
1. يدخل المندوب على شاشة "طلب جديد".
2. يدخل بيانات العميل (الاسم، اسم المشروع، الفرع، فئة المنتج، تاريخ الحجز، المدة بالأيام).
3. يضيف بنود المنتجات (كل منتج مع الكمية المطلوبة).
4. يرفع المرفقات: **أمر الشراء (Purchase Order)** و**كشف الحساب (Account Statement)** (اختياري).
5. عند الإرسال:
   - تُنشأ الحالة `submitted`.
   - يتم حساب `reservationEndDate` من تاريخ الحجز + المدة.
   - يُسجَّل قيد في **سجل الموافقات** و**سجل التدقيق**.
   - تُرسل إشعارات داخل النظام + بريد إلكتروني `request_created` لمديري الفرع المعنيين والمسؤولين.

### 4.2 الحجز الاستراتيجي (مدير التخطيط فقط)
- ينشئ مدير التخطيط الطلب مع `salesChannel = "strategic_reservation"`.
- يبدأ الطلب مباشرة في حالة `category_approved` (يتجاوز موافقة الفرع والصنف).
- يحتاج فقط موافقة مدير القطاع → `final_approved`.
- صرف المخزون يُعتمد تلقائياً بدون مرور بمدير الفرع/التخطيط.

---

## 5. سير عمل الاعتماد (Approval Workflow)

### 5.1 المراحل لطلب عادي
```
مندوب المبيعات
      ↓ (إنشاء)
   submitted
      ↓ (مدير الفرع يعتمد)
 branch_approved
      ↓ (مدير الصنف يعتمد)
category_approved
      ↓ (إذا المدة > 90 يوم: مدير القطاع يعتمد، الحالة لا تتغير لكن يُسجَّل اعتماده)
category_approved (مع توقيع مدير القطاع)
      ↓ (مدير التخطيط يعتمد)
 final_approved
      ↓
يمكن صرف المخزون (جزئي أو كامل)
```

### 5.2 قواعد مهمة في الاعتماد
- **مدير الفرع** يعتمد فقط طلبات فروعه التي عُيّن عليها (يدعم تعدد الفروع).
- **مدير الصنف** يعتمد فقط طلبات فئته.
- **مدير القطاع** مطلوب فقط إذا كانت مدة الحجز > 90 يوم (أو في الحجز الاستراتيجي).
- **مدير التخطيط** لا يستطيع الاعتماد النهائي للطلبات > 90 يوم إلا بعد اعتماد مدير القطاع **بعد آخر رفض** (لا يمكن الالتفاف على مدير القطاع بأي حال).

### 5.3 ما يحدث عند كل اعتماد
1. تتحدث حالة الطلب للمرحلة التالية.
2. يُسجَّل قيد اعتماد في **سجل الموافقات (Approval Trail)** يحتوي: الدور، الإجراء (approve)، الملاحظات، المستخدم، التاريخ.
3. يُسجَّل قيد في **سجل التدقيق (Audit Log)** بالحالة القديمة والجديدة.
4. تُرسل **إشعارات داخل النظام** للمنشئ + المندوب + المعتمد التالي في السلسلة.
5. يُرسل **بريد إلكتروني** بحدث `request_approved` أو `request_final_approved` للأطراف المعنية.

---

## 6. سير عمل الرفض (آلية الإرجاع للمرحلة السابقة)

> عند الرفض، يرجع الطلب للمرحلة السابقة بدلاً من إنهائه — ما عدا رفض المرحلة الأولى الذي يبقى نهائياً.

### 6.1 جدول رفض الطلبات

| من يرفض؟ | الحالة قبل الرفض | الحالة بعد الرفض | يرجع لمن؟ |
|---|---|---|---|
| **مدير الفرع** | `submitted` | `rejected` (نهائي) | المندوب يعدّل ويعيد الإرسال |
| **مدير الصنف** | `branch_approved` | `submitted` | **مدير الفرع** للمراجعة من جديد |
| **مدير القطاع** | `category_approved` | `branch_approved` | **مدير الصنف** للمراجعة من جديد |
| **مدير التخطيط** (مدة > 90 يوم) | `category_approved` | `category_approved` | **مدير القطاع** للمراجعة من جديد |
| **مدير التخطيط** (مدة ≤ 90 يوم) | `category_approved` | `branch_approved` | **مدير الصنف** للمراجعة من جديد |

### 6.2 الحجز الاستراتيجي
إذا رفض مدير القطاع طلب حجز استراتيجي، فالرفض **نهائي** (لأن مدير التخطيط أنشأه ولا يوجد معتمد سابق في السلسلة).

### 6.3 ما يحدث عند الرفض
1. تُحدَّث حالة الطلب للحالة المرتجَع إليها (أو `rejected` إذا كان نهائياً).
2. يُسجَّل في **سجل الموافقات** قيد رفض واضح بالصيغة:
   `Rejected by {الدور}, returned to {الدور السابق} for re-review. Reason: {السبب}`
3. يُسجَّل في **سجل التدقيق** الحالة القديمة، الحالة الجديدة، الدور الرافض، والدور المُرتجع إليه.
4. **إشعارات داخل النظام** تُرسل لـ:
   - منشئ الطلب + مندوب المبيعات (دائماً).
   - معتمدو المرحلة السابقة (مديرو الفرع لذلك الفرع، أو مديرو الصنف لتلك الفئة، أو مديرو القطاع، أو مديرو التخطيط).
5. **بريد إلكتروني** بحدث `request_rejected` لنفس الجهات، والأدوار المُستهدفة تُحدَّد ديناميكياً حسب الحالة المرتجع إليها.

### 6.4 نقطة فنية ذكية
عندما يرفض مدير التخطيط طلباً مدته > 90 يوم، يرجع للحالة `category_approved` ليعتمده مدير القطاع من جديد. الاعتماد السابق لمدير القطاع يُعتبر "مُلغى ضمنياً" بسبب الرفض الذي جاء بعده — فالنظام يسمح له بالاعتماد من جديد، ومدير التخطيط لن يستطيع الاعتماد النهائي إلا بعد توقيع مدير القطاع بعد آخر رفض.

### 6.5 مثال متكامل
طلب مدته 120 يوم:
1. المندوب يُنشئ → `submitted`
2. مدير الفرع يعتمد → `branch_approved`
3. مدير الصنف يعتمد → `category_approved`
4. مدير القطاع يعتمد → `category_approved` (مع تسجيل اعتماده)
5. مدير التخطيط **يرفض** → الحالة ترجع إلى `category_approved`، ويُخطَر مدير القطاع
6. مدير القطاع **يعتمد من جديد** → `category_approved` (اعتماد جديد)
7. مدير التخطيط يعتمد → `final_approved` ✅

---

## 7. سير عمل صرف المخزون (Stock Releases)

### 7.1 إنشاء طلب صرف
- بعد `final_approved`، يستطيع مندوب المبيعات أو منشئ الطلب أن يطلب صرف بنود (جزئي أو كامل لكل منتج).
- يتم اختيار **موقع التخزين (Warehouse Location)** لكل بند.

### 7.2 سلسلة اعتماد الصرف
```
submitted (طلب صرف جديد)
    ↓ (مدير الفرع يعتمد)
branch_approved
    ↓ (مدير التخطيط يعتمد)
final_approved → يُخصم من المخزون فعلياً
```

### 7.3 جدول رفض طلبات الصرف

| من يرفض؟ | بعد الرفض | يرجع لمن؟ |
|---|---|---|
| **مدير الفرع** | `rejected` (نهائي) | منشئ الطلب |
| **مدير التخطيط** | `submitted` | **مدير الفرع** |

### 7.4 ملاحظات
- في الحجز الاستراتيجي يُعتمد طلب الصرف تلقائياً مباشرة.
- يتوفر **تصدير Excel** لطلبات الصرف يحتوي على عمود "موقع التخزين" لكل بند.

---

## 8. سير عمل تمديد الحجز (Extensions)

### 8.1 إنشاء طلب تمديد
- يمكن طلب تمديد قبل أو بعد انتهاء الحجز.
- يُحدَّد التاريخ الجديد المقترح للانتهاء.
- يحسب النظام **إجمالي الأيام من تاريخ الإنشاء** (`totalDaysFromCreation`).

### 8.2 سلسلة اعتماد التمديد
```
pending_branch
    ↓ (مدير الفرع يعتمد)
pending_category
    ↓ (مدير الصنف يعتمد)
pending_planning
    ↓ (إذا الإجمالي > 90 يوم: مدير القطاع يعتمد)
pending_sector_head
    ↓
approved → يُحدَّث reservationEndDate ويزيد extensionCount
```

> إذا الإجمالي ≤ 90 يوم، يُعتمد التمديد مباشرة بعد مدير التخطيط.

### 8.3 جدول رفض طلبات التمديد

| من يرفض؟ | بعد الرفض | يرجع لمن؟ |
|---|---|---|
| **مدير الفرع** | `rejected` (نهائي) | مُقدِّم طلب التمديد |
| **مدير الصنف** | `pending_branch` | **مدير الفرع** |
| **مدير التخطيط** | `pending_category` | **مدير الصنف** |
| **مدير القطاع** | `pending_planning` | **مدير التخطيط** |

### 8.4 ميزات إضافية
- **مدير التخطيط/القطاع/المسؤول** يستطيعون **تعديل تاريخ الانتهاء المقترح** أثناء الاعتماد، ويُسجَّل التعديل في سجل التدقيق.
- عند اعتماد تمديد نهائياً، تُرفض تلقائياً أي طلبات تمديد أخرى مفتوحة لنفس الطلب.
- إذا كان الطلب `expired` ثم اعتُمد تمديده → يعود لحالة `final_approved`.

---

## 9. تعديل الطلب (Edit & Change Tracking)

### 9.1 من يستطيع التعديل؟
| الدور | متى يستطيع التعديل؟ |
|---|---|
| **مندوب المبيعات** | فقط طلباته في حالة `rejected` |
| **مدير الفرع** | الطلبات في حالة `submitted` |
| **مدير الصنف** | الطلبات في حالة `branch_approved` |
| **مدير التخطيط** | الطلبات في حالة `category_approved` أو `final_approved` |
| **المسؤول** | أي طلب (إلا `expired` أو `lost_opportunity`) |

### 9.2 تتبع التغييرات
- **البنود الجديدة** تُعلَّم بشارة "جديد" خضراء + خلفية صف خضراء.
- **البنود المعدَّلة** (تغيير الكمية) تُعلَّم بشارة "معدّل" برتقالية + خلفية صف برتقالية + يُعرض الكمية القديمة مشطوبة بجانب الجديدة.
- في **سجل الموافقات**، قيد التعديل يعرض جدول مقارنة (الحقل، القيمة قبل، القيمة بعد) + جدول تغييرات البنود (مُضاف/محذوف/معدّل).
- **بريد إلكتروني** `request_edited` يُرسل لجميع المعنيين مع جدول الحقول المُغيَّرة.

### 9.3 حماية مهمة
عند تعديل التخطيط/المسؤول للطلب، لا يُعاد حساب `reservationEndDate` إلا إذا تغيّر `requestDate` أو `reservationDuration` فعلاً — هذا يمنع تحويل الطلب إلى `expired` بطريق الخطأ.

---

## 10. الإشعارات (Notifications)

### 10.1 الإشعارات داخل النظام
- شارة بأيقونة الجرس في الشريط العلوي تُظهر عدد الإشعارات غير المقروءة.
- صفحة كاملة لعرض جميع الإشعارات مع إمكانية وضع علامة "مقروء" / "تعليم الكل كمقروء".
- كل إشعار يحتوي رابطاً مباشراً لشاشة الطلب المعني.

### 10.2 من يحصل على الإشعارات؟
- منشئ الطلب ومندوب المبيعات: في كل تغيير على الطلب.
- المعتمد التالي في السلسلة: عند انتقال الطلب إلى مرحلته.
- المعتمد السابق: عند رفض يُرجع الطلب لمرحلته.
- المسؤولون: في معظم الأحداث المهمة.
- مديرو التخطيط: عند الأحداث التي تتطلب تدخّلهم (تنبيهات الانتهاء، طلبات الصرف).

### 10.3 تنبيهات قُرب انتهاء الحجز
- مهمة مجدولة تعمل كل 6 ساعات.
- تفحص الطلبات في حالة `final_approved` التي تنتهي خلال 3 أيام مع وجود بنود غير مصروفة.
- ترسل إشعار + بريد إلكتروني للمنشئ والمندوب ومديري الفرع والتخطيط والمسؤولين.
- شارة "قريب من الانتهاء" حمراء على بطاقة الطلب في القائمة.
- بانر تحذيري أحمر في صفحة تفاصيل الطلب.
- تجنّب التكرار: إشعار واحد لكل طلب في اليوم.

---

## 11. البريد الإلكتروني

### 11.1 المزود والإرسال
- مزود الخدمة: **Resend**.
- المرسِل: `Bayt Alebaa Operations <no-reply@ebaaops.com>`.
- قوالب HTML احترافية، ثنائية اللغة (عربي/إنجليزي).
- شارات حالة ملوّنة + جداول مقارنة الحقول المُغيَّرة.

### 11.2 الأحداث المدعومة
- `request_created` — إنشاء طلب جديد
- `request_approved` — اعتماد في مرحلة وسطى
- `request_final_approved` — الاعتماد النهائي
- `request_rejected` — رفض / إرجاع لمرحلة سابقة
- `request_edited` — تعديل الطلب
- `stock_release_requested` — طلب صرف جديد
- `stock_release_approved` — اعتماد طلب صرف
- `stock_release_final_approved` — الصرف الفعلي
- `stock_release_rejected` — رفض طلب صرف
- `extension_*` — أحداث التمديد المماثلة

### 11.3 موثوقية الإرسال
- **إرسال غير حاجب**: لا يؤخر استجابة الـ API.
- **إعادة محاولة**: 3 محاولات بفترات تصاعدية (2ث، 5ث، 10ث).
- **حد معدل**: 10 رسائل في الدقيقة لكل مستلم.
- **سجل**: كل بريد يُسجَّل في جدول `email_logs` (المستلم، الموضوع، الحدث، الحالة، الأخطاء، عدد المحاولات).

### 11.4 تفضيلات المستخدم
- صفحة `/email-preferences` لكل مستخدم: يفعّل/يعطّل الإشعارات لكل نوع حدث.
- صفحة `/email-logs` للمسؤولين فقط: عرض كل الرسائل المُرسلة مع فلاتر (نوع الحدث، الحالة، المستلم) وترقيم صفحات.

---

## 12. التقارير والتحليلات (Planning/Admin فقط)

### 12.1 التقارير الرئيسية
- **التقرير التنفيذي** (`/reports/executive-overview`): مؤشرات أداء عليا مع فلتر زمني.
- **التعرّض المالي** (`/reports/financial-exposure`): مؤشرات + رسوم بيانية حسب الفرع/الفئة + شرائح زمنية (Aging Buckets) + تصدير Excel.
- **ذكاء الأرباح** (`/reports/profit`): مؤشرات + رسوم حسب الفرع والمندوب + أعلى المنتجات.
- **تقرير التخطيط** (`/planning-reports`): KPIs + رسوم + تحليل المخاطر + تصدير Excel (يحتوي عمود موقع التخزين).

### 12.2 التقارير حسب البُعد
- حسب العميل / المشروع / الفرع / العلامة التجارية / القسم / الفئة / المنتج.
- جداول قابلة للترتيب والبحث.

### 12.3 تحليلات العملاء والمشاريع
- صفحة قائمة العملاء (`/customers`) مع KPIs ملخّصة وفلاتر.
- صفحة تفاصيل عميل تحتوي مؤشرات + جدول طلبات.
- نفس النمط للمشاريع (`/projects`) مع تبويبات تقارير فرعية.

### 12.4 جدول المنتجات في تفاصيل الطلب
- يعرض لكل منتج: الكمية المطلوبة، المصروفة، المتبقية، السعر، **إجمالي بيع المتبقي** (المتبقي × سعر البيع).
- تذييل بإجمالي كل عمود.

---

## 13. سجل التدقيق (Audit Log)

- يُسجَّل كل إجراء حسّاس: إنشاء، تعديل، اعتماد، رفض، صرف، تمديد، تغيير تاريخ انتهاء، رفع مرفقات.
- يحتوي: من قام بالإجراء، الوصف، الكيان، معرف الكيان، البيانات القديمة والجديدة، التوقيت.
- متاح للمسؤولين عبر `/audit-logs` مع فلترة وترقيم صفحات.

---

## 14. إدارة الملفات والمرفقات

- يتم رفع المرفقات (أمر شراء، كشف حساب) إلى **التخزين السحابي (Object Storage)** (تخزين سحابي دائم — يبقى بعد عمليات النشر).
- نقطة الرفع: `POST /api/upload/purchase-order` و`POST /api/upload/account-statement`.
- نقطة العرض: `GET /cloud-uploads/<bucket>/.private/uploads/<filename>` مع توثيق JWT.
- الملفات القديمة (قبل الترحيل) تُعرَض من المسار المحلي القديم `/uploads/:filename` للتوافق.

---

## 15. الواجهة ثنائية اللغة (i18n)

- دعم كامل للعربي والإنجليزي.
- زر تبديل اللغة في الشريط العلوي وصفحة تسجيل الدخول.
- التبديل بين RTL وLTR ديناميكي على مستوى `<html>` (الـ Sidebar يتقلب جهته، الأيقونات الاتجاهية كأسهم الرجوع تنعكس).
- اكتشاف لغة المتصفح تلقائياً + حفظ الاختيار في `localStorage` (مفتاح `app-lang`).
- قاموس مركزي للترجمات في `/client/src/lib/i18n.tsx`.

---

## 16. الحسابات التجريبية (كلمة المرور: `password123`)

| الدور | البريد |
|---|---|
| مندوب مبيعات | `ahmed@baitiba.com` |
| مندوب مبيعات 2 | `fahad@baitiba.com` |
| مدير فرع | `khalid@baitiba.com` |
| مدير فرع 2 | `omar@baitiba.com` |
| مدير صنف | `nora@baitiba.com` |
| مدير تخطيط | `sultan@baitiba.com` |
| مدير قطاع | `mansour@baitiba.com` |
| مسؤول | `admin@baitiba.com` |

---

## 17. ملخّص دورة حياة الطلب

```
┌─────────────────┐
│  إنشاء الطلب     │ ← مندوب المبيعات / مدير التخطيط (استراتيجي)
└────────┬────────┘
         ↓
┌─────────────────┐    رفض من مدير الفرع
│   submitted     │ ─────────────────────→  rejected (نهائي، يعدّل المندوب ويعيد)
└────────┬────────┘
         ↓ اعتماد مدير الفرع
┌─────────────────┐    رفض من مدير الصنف
│ branch_approved │ ────────────────────→  submitted (يرجع لمدير الفرع)
└────────┬────────┘
         ↓ اعتماد مدير الصنف
┌──────────────────┐    رفض من مدير القطاع
│category_approved │ ───────────────────→ branch_approved (يرجع لمدير الصنف)
│  (مدة>90: ينتظر  │
│   مدير القطاع)   │    رفض من مدير التخطيط (مدة≤90)
│                  │ ───────────────────→ branch_approved
│                  │    رفض من مدير التخطيط (مدة>90)
│                  │ ───────────────────→ category_approved (يرجع لمدير القطاع)
└────────┬─────────┘
         ↓ اعتماد مدير التخطيط (بعد مدير القطاع إن لزم)
┌─────────────────┐
│ final_approved  │ → صرف المخزون (جزئي أو كامل) → تمديد عند الحاجة
└────────┬────────┘
         ↓ إذا انتهت المدة وفيه بنود لم تُصرف
┌─────────────────┐    لم يُجدَّد خلال 3 أيام
│    expired      │ ──────────────────→  lost_opportunity (تلقائي)
└─────────────────┘
```

---

## 18. خلاصة الميزات الرئيسية

| الميزة | الوصف |
|---|---|
| **اعتماد متعدد المراحل** | 3 إلى 4 مستويات حسب مدة الحجز |
| **رفض ذكي** | يرجع للمرحلة السابقة بدلاً من الإنهاء |
| **تتبع كامل** | سجل موافقات + سجل تدقيق لكل عملية |
| **إشعارات شاملة** | داخل النظام + بريد إلكتروني مع تفضيلات لكل مستخدم |
| **تتبع التغييرات** | تمييز بصري للبنود الجديدة والمعدلة + جداول مقارنة |
| **تنبيهات الانتهاء** | تنبيه تلقائي قبل 3 أيام من انتهاء الحجز |
| **تمديد الحجوزات** | سلسلة اعتماد منفصلة مع نفس آلية الرفض |
| **تقارير غنية** | مؤشرات أداء + رسوم + تصدير Excel + تحليل بأبعاد متعددة |
| **ثنائية اللغة** | عربي/إنجليزي مع RTL/LTR كامل |
| **تخزين سحابي** | المرفقات في تخزين دائم (Object Storage) |
| **حجز استراتيجي** | تدفق مختصر لمدير التخطيط |

---

*نهاية الدليل.*
